// Animation and transition durations shared across features.

/// Central durations for Flutter animations and switchers.
class AppDurations {
  AppDurations._();

  static const Duration hubTabFade = Duration(milliseconds: 240);
}
