import 'package:flutter/widgets.dart';

/// Web / conditional stub — no filesystem access.
Widget? implLoadStoryboardDesktopImage(
  String filename, {
  required String basePath,
  BoxFit fit = BoxFit.cover, // Ignored without dart:io
}) => null;
