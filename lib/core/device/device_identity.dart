/// Stable device id + human-readable name for auth and push registration.
class DeviceIdentity {
  final String id;
  final String name;

  const DeviceIdentity({required this.id, required this.name});
}
